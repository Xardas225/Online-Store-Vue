const getters = {
    PRODUCTS(state) {
        return state.products;
    },
    CART(state) {
        return state.cart;
    },
    HOME_DATA(state) {
        return state.home_data;
    }
}

export default getters